# Changelog

このプロジェクトの変更履歴。バージョニングは [Semantic Versioning](https://semver.org/lang/ja/) に従う。

## [0.4.0] - 2026-09-26

GrabQv のまとまりを Erase ボタンで丸ごと消せるようにし、持ち方を選べるようにした。

### Added
- **GrabQv**: ペンの Erase ボタンの長押しに、まとまりを丸ごと消す段階を追加した。0.31 秒未満で離すと Undo、0.31 秒で自分が最後に描いたまとまり（ほかの人が描き足した線を含む）、1 秒で自分の線すべて、2 秒でそのペンの線すべてが消える。
  - ボタンの左上に、SELF・ALL の輪に加えて SPLIT の輪を表示する。
  - GrabQv の対象のペン（BodyQv + GrabQv のペンを含む）の Erase ボタンだけを差し替える。差し替え・元に戻すは `GrabQvManager` の Inspector のボタンで行う。`GrabQv` / `BodyGrabQv` の Prefab は差し替え済み。
- **GrabQv**: `GrabQvManager` に「押すたびに持つ／離す」を追加した。ON で押すと持ち、もう一度押すと離す。OFF で押している間だけ持つ。

### Changed
- **GrabQv**: `GrabQv` の Prefab の持ち方を、押すたびに持つ／離すにした。`BodyGrabQv` の Prefab は、これまでどおり押している間だけ持つ。

## [0.3.0] - 2026-09-26

BodyQv と GrabQv を 1 本のペンに組み合わせられるようにした。

### Added
- **BodyQv + GrabQv**: 同じペンを BodyQv と GrabQv の両方の対象にすると、描いたものが体に付いて追従し、手で持って位置を直せるようになる。
  - まとまりの最初の線で体への付け先を決め、持って離したときに、離した場所で付け先を決め直す（体から離れていれば浮いたまま、体の近くならその部位に付き、別の部位・別の人に付け替えることもできる）。
  - 離したときは、持っていた手の腕を付け先の候補から外す。
  - 持てるまとまりの数を超えたまとまりも、体への追従は効く。
  - 組み合わせ用の管理は無く、2 つの管理のつながりはエディタが自動で設定する。
  - QvPen 一式と両方の管理を含む Prefab（`BodyGrabQv`）を追加した。
- **BodyQv**: 翼・尻尾・長い髪など、体から大きくはみ出す線も、線の描き始めか描き終わりが体に触れていれば、その部位に付けるようにした。

### Changed
- **GrabQv**: 持ち手を、各自の画面で描いたものに合わせる「掴む物体」と、持っている人だけが動かす「同期する持ち手」に分けた。GrabQv だけのペンの使い方は変わらない。
- 同じペンを BodyQv と GrabQv の両方の対象にしたときの警告をやめ、組み合わせて動く旨の案内を表示するようにした。

## [0.2.0] - 2026-09-25

最初の公開版。QvPen の拡張として、体に描いた線を追従させる BodyQv と、描いたものを持てるようにする GrabQv の 2 つの機能を収録した。

### Added
- **BodyQv**: 対象の QvPen で体の上に描いた線を、近くにいる人の体の部位に紐付け、その部位の動きに追従させる。自分の体にも、ほかの人の体にも描ける。
  - 線から体の表面までの平均距離で付け先を決める。体から離れた空中の線は付けず、ふつうの QvPen と同じくその場に残す。
  - 顔に描いた線は首や胸より頭を優先する。頭の上に浮かせて描いた猫耳・ヘイローや、顔から描き始めた猫ひげ・涙も頭に付く。
  - 描いている本人のペンを持っている腕には付けない（空中の小さな線が自分の手に付くのを防ぐ）。
  - ヒューマノイドでないアバターには、頭・両手・足元に付ける。
  - ペンを持っている間、近くの人の体に当たり判定を置き、QvPen の Surftrace で体の表面をなぞって描けるようにする（ON/OFF 可）。必要な衝突の設定は、Inspector のボタンを押したときだけ、該当する行だけを直す。
  - 確認用に、線の付け先と判定の中身をログに出す設定と、当たり判定の形を表示する設定を用意した。
- **GrabQv**: 対象の QvPen で描いた線を、まとまりごとに誰でも手で持って動かせるようにする。
  - ペンの Erase ボタンの下に Split ボタンを付け、押すまでに描いた線を 1 つのまとまりにする。今のまとまりから離れた場所に描いた線は自動で別のまとまりにする。
  - 持ち手は Prefab に 32 個用意し、使い回す。まとまりの線がすべて消えると持ち手を空ける。
- 後から入った人にも、線の付け先・まとまりの位置を同期する。
- QvPen 一式を含む Prefab（`BodyQv` / `GrabQv`）と、手持ちの QvPen を対象に指定するための管理だけの Prefab を用意した。
- 使い方ドキュメント: [https://ghp.maaaaa.net/EXQv/](https://ghp.maaaaa.net/EXQv/)
